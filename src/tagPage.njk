---
eleventyComputed:
  title: "Archive: {{tag}}"
layout: layouts/page.njk
permalink: /tags/{{ tag }}/
pagination:
  data: collections
  size: 1
  alias: tag
eleventyExcludeFromCollections: true
---

{% set taglist = collections[ tag ] %}

<p>Here are all the posts tagged <b>{{ tag }}</b>:</p>

<ul>
  {%- for post in taglist | reverse -%}
    <li><a href="{{ post.url }}">{{ post.data.title }}</a> ({{ post.date | formatDate }})</li>
  {%- endfor -%}
</ul>
